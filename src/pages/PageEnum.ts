export enum PageNames {
  Null = 'null',
  Home = 'home',
  Text = 'text',
  File = 'file',
  Mode = 'mode',
  User = 'user',
}

export enum PagePaths {
  Null = 'null',
  Home = '/home',
  Text = '/text',
  File = '/file',
  Mode = '/mode',
  User = '/user',
}
